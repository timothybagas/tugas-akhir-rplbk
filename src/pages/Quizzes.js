import React from "react";
import Template from "../components/Template";

export default function Quizzes() {
    return (
        <Template>
            <h1 className="text-slate-700">Quizzes</h1>
        </Template>
    );
}